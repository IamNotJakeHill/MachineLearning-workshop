import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.preprocessing import LabelEncoder, StandardScaler
from scipy.cluster.hierarchy import dendrogram, linkage
from sklearn.cluster import KMeans

data = pd.read_csv('countries of the world.csv', decimal=',')
print(data.head())
print(data.info())

data_numeric = data.drop(['Country', 'Region'], axis=1)
label_encoder = LabelEncoder()
data['Country'] = label_encoder.fit_transform(data['Country'])
data['Region'] = label_encoder.fit_transform(data['Region'])

scaler = StandardScaler()
data_numeric = scaler.fit_transform(data_numeric)
data_numeric = pd.DataFrame(data_numeric, columns=data_numeric.columns)

# Przygotowanie danych do analizy skupień
X = data.iloc[:, 1:].values

# Wykonanie klasteryzacji za pomocą algorytmu K-means
kmeans = KMeans(n_clusters=3, random_state=0)
kmeans.fit(X)

# Sprawdzenie sugerowanej liczby k (inertia)
inertias = []
for k in range(1, 11):
    kmeans = KMeans(n_clusters=k, random_state=0)
    kmeans.fit(X)
    inertias.append(kmeans.inertia_)

# Wykres sugerowanej liczby k (inertia)
plt.plot(range(1, 11), inertias, marker='o')
plt.xlabel('Liczba klastrów (k)')
plt.ylabel('Wartość inertia')
plt.title('Metoda łokcia - wybór liczby klastrów')
plt.show()

# Budowa dendrogramu
linkage_matrix = linkage(X, method='ward')
dendrogram(linkage_matrix, truncate_mode='level', p=3)
plt.xlabel('Indeksy próbek')
plt.ylabel('Odległość')
plt.title('Dendrogram')
plt.show()
